#include "fila.h"

// void imprimirInt(void *info) {
//     int *valor = (int*)info; // Converte o ponteiro para int*
//     printf("%d ", *valor);
// }

int main() {

    // Fila *F = criarNo((void*)0);
    
    // inserirFila(F, (void*)1);
    // inserirFila(F, (void*)2);
    // inserirFila(F, (void*)3);
    // imprimirFila(F, imprimirInt);

    // retirarDaFila(F);
    // retirarDaFila(F);
    // imprimirFila(F, imprimirInt);
    
    // destruirFila(F);
    // imprimirFila(F, imprimirInt);
    
    return 0;
}